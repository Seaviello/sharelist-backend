type User {
    id: ID! @unique
    name: String!
    email: String!
}

type Task {
    id: ID! @unique
    createdAt: DateTime!
    title: String!
    # creator: User!
    # owner: User!
    # updatedAt: DateTime!
    # expireAt: DateTime!
    # description: String!
}
